tmux:
  session: twitter-clone
  window: dev

services:
  frontend:
    command: npm run dev
    port: 3000
    setup: npm install

  emulators:
    command: npm run emulators
    setup: firebase login